{% set name = "bvalcalc" %}
{% set version = "1.3.0" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 419b69bfd34d996c10d2b59a4cd5c9d97d79c68c918666945dc2eb3783d44f74

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-build-isolation --no-deps
  entry_points:
    - bvalcalc = Bvalcalc.cli:main
    - Bvalcalc = Bvalcalc.cli:main

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools >=61.0
    - wheel
  run:
    - python >={{ python_min }}
    - numpy >=1.22
    - scipy >=1.13
    - matplotlib-base
    - seaborn >=0.13.2,<0.14.0

test:
  imports:
    - Bvalcalc
    - Bvalcalc.core
    - Bvalcalc.utils
  commands:
    - pip check
    - bvalcalc --help
    - Bvalcalc --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/JohriLab/Bvalcalc
  license: GPL-3.0-only
  license_file: LICENSE
  license_family: GPL
  summary: Calculate relative diversity (B) under background selection
  doc_url: https://JohriLab.github.io/Bvalcalc/
  dev_url: https://github.com/JohriLab/Bvalcalc

extra:
  recipe-maintainers:
    - jacobimarsh
